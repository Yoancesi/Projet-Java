package controller;

import static org.junit.Assert.*;

import javax.sound.sampled.Control;
import javax.swing.text.View;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

import contract.IModel;
import contract.IView;
import view.string;


public class ControllerTest {


	private Controller controller;
	private contract.IView		IView;
	private model	IModel;
	
	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
	}

	@AfterClass
	public static void tearDownAfterClass() throws Exception {
	}

	@Before
	public void setUp() throws Exception {
	}

	@After
	public void tearDown() throws Exception {
	}

	@Test
	public void testControl() {
		
		final String expected = "Appuyer sur les touches 'E', 'F', 'D' ou 'I', pour afficher Hello world dans la langue d votre choix.";
		assertEquals(expected, this.controller.control());
		
		//fail("Not yet implemented");
	}

	@Test
	public void testsetView() {
		
		final String expected = "pview";
		assertEquals(expected, this.controller.setView(IView));
		
		//fail("Not yet implemented");
	}
	
	@Test
	public void testsetModel() {
		
		final String expected = "model";
		assertEquals(expected, this.controller.setModel(IModel));
		
		//fail("Not yet implemented");
	}

	
}
